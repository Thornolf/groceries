<%= simple_form_for @item do |f| %>
  <%= f.input :title, label: "Item", placeholder: "Pizza, salmon, milk..." %>
  <%= f.input :description, placeholder: 'Add quantity, brand etc...'%>
  <%= f.button :submit, "Add to the cart"%>
<% end %>